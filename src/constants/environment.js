/* eslint-disable no-undef */
export const env =
  process.env.NODE_ENV === "production"
    ? "https://widget.payper.ca/"
    : "http://localhost:4000/";
    // ? "http://localhost:4000/"
    // : "http://localhost:4000/";
